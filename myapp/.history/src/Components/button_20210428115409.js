import React from 'react'

const button = ({name}) => {
    return (
        <div>
            <button className='btn'>{}}</button>
        </div>
    )
}

button.defultProps = {
    name: "button"
}

export default button
